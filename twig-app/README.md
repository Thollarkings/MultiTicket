# Twig Ticket Management App

A full-featured ticket management system built with Twig templating engine and PHP.

## Setup Instructions

1. **Install Dependencies**:
   ```bash
   cd twig-app
   composer install